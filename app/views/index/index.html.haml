%h1 Index#index

- @news.each do |news|
    .aricle.col-sm-12
        %h2
            = link_to news.title, :controller => "news", :action => "show", :id => news
        .time.pull-right
            = news.updated_at.to_time.to_formatted_s(:long)
        .content
            %div
                = truncate_html news.content, :length => 30

= paginate @news