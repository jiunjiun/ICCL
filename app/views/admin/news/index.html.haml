%a{:href => new_admin_news_path} New
%table
    %tr
        %th
        %th
        %th title
        %th line
        %th status
    - @news.each do |news|
        %tr
            %td
                =link_to 'Edit', edit_admin_news_path(news)
            %td
                =link_to 'delete', admin_news_path(news), :method => :delete
            %td= news.title
            %td= news.line
            %td= news.status
