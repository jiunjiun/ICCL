%a{:href => new_admin_study_path} New
%table
    %tr
        %th
        %th
        %th title
        %th line
        %th classes
        %th status
    - @studies.each do |study|
        %tr
            %td
                =link_to 'Edit', edit_admin_study_path(study)
            %td
                =link_to 'delete', admin_study_path(study), :method => :delete
            %td= study.title
            %td= study.line
            %td= study.classes
            %td= study.status
