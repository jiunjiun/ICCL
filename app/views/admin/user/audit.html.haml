%table
    %tr
        %th
        %th role
        %th classes
        %th name
    - @users.each do |user|
        %tr
            %td
                %a{:href=> "verify/#{user.id}"} Audit
            %td= user.role
            %td= user.classes
            %td= user.name


