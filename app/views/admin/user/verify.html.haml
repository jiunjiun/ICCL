%table
    %tr
        %th
        %th role
        %th classes
        %th name
    - @users.each do |user|
        %tr
            %td= user.id

            %td= user.role
            %td= user.classes
            %td= user.name


